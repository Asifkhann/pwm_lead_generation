{
  "name": "lead-generation-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Lead Generation & Sales Management Dashboard for Perfect Web Metrix",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "dev": "npm-run-all --parallel dev:server dev:client",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run build",
    "test": "npm --prefix server test && npm --prefix client test",
    "test:server": "npm --prefix server test",
    "test:client": "npm --prefix client test",
    "build:deploy": "npm --prefix server run build && npm --prefix client run build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
