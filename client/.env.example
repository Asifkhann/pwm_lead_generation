# Base URL for the API. Leave empty in development to use the Vite dev proxy.
VITE_API_BASE_URL=

# Dev server port
VITE_PORT=5173

# Where the Vite dev proxy forwards /api requests
VITE_API_PROXY_TARGET=http://localhost:5050
